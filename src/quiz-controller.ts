class Quiz  {
    constructor(
        public questions: string,
        public score: number,
        public questionIndex: number,
    ) {

    }
}

// Quiz.endsHere = function () {
//     return this.questions[this.questionIndex];
// }

// Quiz..userGuess = function(answer) {
//     this.questionIndex++;

//     if(this.questionIndex() .correct)
